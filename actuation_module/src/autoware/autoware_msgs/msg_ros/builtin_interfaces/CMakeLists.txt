# Phase 1B nros-codegen stub. nros_generate_interfaces auto-picks
# msg/*.msg from this dir. No DEPENDENCIES — builtin_interfaces is the
# root of the dep DAG.
nros_generate_interfaces(builtin_interfaces LANGUAGE CPP)
